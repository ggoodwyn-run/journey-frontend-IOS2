import SwiftUI

// MARK: - Color Scheme (Dark Blue Theme - Refined)
struct JourneyColors {
    // Primary blues - main interaction colors
    static let primary = Color(red: 0.3, green: 0.6, blue: 0.95) // Soft sky blue
    static let secondary = Color(red: 0.4, green: 0.7, blue: 1.0) // Light sky blue
    
    // Accent - use sparingly for emphasis
    static let accent = Color(red: 0.99, green: 0.6, blue: 0.3) // Soft coral (less aggressive)
    static let success = Color(red: 0.3, green: 0.85, blue: 0.65) // Soft teal green
    
    // Background colors - dark but not too harsh
    static let background = Color(red: 0.1, green: 0.15, blue: 0.24) // Deep navy (softer)
    static let cardBackground = Color(red: 0.14, green: 0.19, blue: 0.3) // Dark blue-gray
    
    // Text colors - improved contrast and readability
    static let textPrimary = Color(red: 0.98, green: 0.99, blue: 1.0) // Pure white
    static let textSecondary = Color(red: 0.75, green: 0.8, blue: 0.9) // Light blue-gray (brighter)
    static let textTertiary = Color(red: 0.6, green: 0.65, blue: 0.75) // Medium blue-gray for subtle text
}
